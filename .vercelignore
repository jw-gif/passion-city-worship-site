# Not part of the site: run these by hand in the Supabase SQL editor.
sql/

# content/fallback.json is deployed again. It is a copy of the handbook, and
# the handbook is readable by anyone with the link, so publishing it gives
# nothing away that / does not — and it is what the site falls back to when
# Supabase is unreachable.
